# 不需要进入 Docker 构建上下文的文件 — 缩短传输 + 防止本地构建产物污染镜像。

# Node
**/node_modules
**/.pnpm-store
**/.vite
**/.turbo
**/.next
**/.cache

# Go
**/bin
**/*.exe
upstream-ops
upstream-ops.exe

# 本地构建产物：Dockerfile 内部会重新 pnpm build，不要把宿主的 dist 拷进去
frontend/dist
frontend/test-results
frontend/playwright-report

# 嵌入占位以外的本地 dist 内容
web/dist/*
!web/dist/.gitkeep

# 版本控制 / IDE
.git
.gitignore
.gitattributes
.github
.idea
.vscode
**/*.log
*.swp

# Env / config
.env
.env.local
config.yaml

# Volumes / 缓存
data/
postgres-data/
.claude/

# 本地部署与离线镜像产物，不参与应用镜像构建
napcat-chunks/
napcat-image.tgz
napcat-qrcode.png
upstream-ops-image.tgz
upstream-ops-*.tgz
upstream-ops-*.tar
_preview_proxy.py

# 文档（构建不需要）
docs/
README.md
CLAUDE.md
*.md
