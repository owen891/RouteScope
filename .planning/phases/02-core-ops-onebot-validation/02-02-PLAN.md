---
phase: 02-core-ops-onebot-validation
plan: "02"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/components/monitor/channel-cards.tsx
  - frontend/lib/channel-error.ts
  - frontend/lib/import-and-error.test.ts
requirements: [FAIL-01, FAIL-02, FAIL-03]
autonomous: true
---

<objective>
Turn failed-channel diagnosis into a recoverable workflow with filtered targets, bounded batch mutations, and per-channel results.
</objective>

<must_haves>
  - Failure filters remain grouped by fingerprint, expired token, Turnstile, password, network, and other.
  - “Sync failed” operates only on the active failed filter and continues after an individual failure.
  - Batch password mode and batch password update show each target's success or failure rather than only an aggregate toast.
  - Fingerprint/token failures provide direct entry points for password or token credential editing; Turnstile failures provide the captcha route.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure recovery target/result helpers</name>
  <files>frontend/lib/channel-error.ts, frontend/lib/import-and-error.test.ts</files>
  <action>Add small pure helpers for selecting the current failure cohort and normalizing per-item batch outcomes. Keep classification backward-compatible with existing error strings and tests.</action>
  <verify>pnpm.cmd --dir frontend test</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace aggregate-only batch feedback</name>
  <files>frontend/components/monitor/channel-cards.tsx</files>
  <action>Keep the existing confirmation and sequential API writes, but retain a result list in the page/dialog so each channel reports success or its actionable error. Disable overlapping recovery actions while a batch is active and preserve the active failure filter after completion.</action>
  <verify>pnpm.cmd --dir frontend exec tsc --noEmit --incremental false && pnpm.cmd --dir frontend lint</verify>
</task>

</tasks>

<acceptance_criteria>
  - An operator can filter failures, run only the selected cohort, and see per-channel recovery results.
  - A single failed mutation does not prevent later channels from being attempted.
  - Existing clear-login, password-mode, token-mode, and captcha entry points remain intact.
</acceptance_criteria>
