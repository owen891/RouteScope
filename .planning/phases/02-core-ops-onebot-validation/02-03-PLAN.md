---
phase: 02-core-ops-onebot-validation
plan: "03"
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - frontend/components/monitor/notification-form-dialog.tsx
  - backend/notify/qqbot.go
  - backend/notify/qqbot_test.go
  - frontend/lib/notify-test-error.ts
requirements: [QQNT-01, QQNT-02]
autonomous: true
---

<objective>
Complete the OneBot operator path for group/private targets, Bearer/query authentication, and actionable Docker/network errors.
</objective>

<must_haves>
  - The notification form exposes group/private target selection, access token, and explicit Bearer versus query authentication.
  - Backend query authentication uses standards-compliant URL encoding for arbitrary token characters.
  - Group and private requests encode numeric IDs as numbers when possible and preserve string IDs otherwise.
  - HTTP status failures, nonzero OneBot retcodes/statuses, missing targets, auth failures, and Docker loopback mistakes map to actionable UI messages.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add query-auth control and preserve edit semantics</name>
  <files>frontend/components/monitor/notification-form-dialog.tsx</files>
  <action>Add a boolean query-auth field to the QQ form state and serialization, retain it when editing, and keep the current safe Docker host default and write-only token input behavior.</action>
  <verify>pnpm.cmd --dir frontend exec tsc --noEmit --incremental false && pnpm.cmd --dir frontend lint</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden OneBot transport and tests</name>
  <files>backend/notify/qqbot.go, backend/notify/qqbot_test.go, frontend/lib/notify-test-error.ts</files>
  <action>Replace manual query escaping with net/url encoding, add tests for reserved token characters and both target modes, and preserve actionable error categories for HTTP, retcode, auth, target, and Docker connectivity failures.</action>
  <verify>go test ./backend/notify -count=1 && pnpm.cmd --dir frontend test</verify>
</task>

</tasks>

<acceptance_criteria>
  - The operator can configure and test both OneBot group and private messages using either Bearer or query auth.
  - The test path reports actionable failures without exposing stored access tokens.
  - OneBot transport tests cover HTTP errors, business retcodes, target validation, and reserved-character query tokens.
</acceptance_criteria>
