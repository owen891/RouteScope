---
phase: 02-core-ops-onebot-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/monitor/channel-import-dialog.tsx
  - frontend/lib/all-api-hub-import.ts
  - frontend/lib/import-and-error.test.ts
  - backend/api/channels_test.go
requirements: [IMPT-01, IMPT-02, IMPT-03, IMPT-04]
autonomous: true
---

<objective>
Make the browser-only all-api-hub import flow auditable from preview through partial writes and optional synchronization.
</objective>

<must_haves>
  - The original backup is parsed locally and never uploaded as a whole document.
  - Every row has a deterministic create/update/rename/skip/error decision before writing.
  - A failed row does not stop later rows; results distinguish created, updated, skipped, and failed rows.
  - Only channel IDs successfully written in the current run are sent to the optional sync path.
  - Notes-derived passwords enter the existing encrypted credential field and are absent from login metadata.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the import decision and credential boundary</name>
  <files>frontend/lib/all-api-hub-import.ts, frontend/lib/import-and-error.test.ts</files>
  <action>Extend pure parser tests for name and URL conflicts, malformed rows, disabled rows, expired-token fallback, notes-password fallback, and metadata redaction. Keep the parser deterministic and ensure every payload contains only the credential field needed by its selected mode.</action>
  <verify>pnpm.cmd --dir frontend test</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make partial import outcomes explicit</name>
  <files>frontend/components/monitor/channel-import-dialog.tsx, backend/api/channels_test.go</files>
  <action>Preserve the current sequential write behavior but expose row-level outcome labels and written IDs consistently. Add a backend contract test that confirms create/update payloads are accepted without plaintext notes metadata and that subsequent sync selection is based only on successful IDs. Do not add a bulk backend endpoint unless tests show the current contract cannot express the required behavior.</action>
  <verify>pnpm.cmd --dir frontend exec tsc --noEmit --incremental false && go test ./backend/api -count=1</verify>
</task>

</tasks>

<acceptance_criteria>
  - A local backup can be previewed, conflict decisions are visible, and malformed rows do not block valid rows.
  - Import results show each row's write outcome and optional sync never includes failed or skipped rows.
  - No plaintext notes password is stored in login_extra_params or sent as a comment field.
</acceptance_criteria>
