---
phase: 01-automated-gates-production-safety
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config/config.go
  - backend/config/config_test.go
  - backend/runtimeconfig/runtime.go
  - backend/runtimeconfig/runtime_test.go
  - cmd/server/main.go
  - backend/api/settings.go
  - backend/api/settings_test.go
  - frontend/lib/api-types.ts
  - frontend/app/settings-page.tsx
  - backend/auth/auth_test.go
  - scripts/print-auth-env.sh
  - scripts/print-auth-env.ps1
  - scripts/check-production.sh
  - scripts/check-production.ps1
autonomous: true
requirements: [SECU-01, SECU-02, SECU-03]
must_haves:
  truths:
    - "Every currently bound environment-provided deployment value remains authoritative after Settings read, save, and runtime apply."
    - "Settings responses never disclose administrator passwords, token signing secrets, database passwords, proxy passwords, or APP_SECRET, and saving unrelated settings never copies effective environment secrets into YAML."
    - "Generated config files use restrictive permissions, while explicit write-only secret replacements remain possible without returning the stored value."
    - "Credential helpers generate a strong administrator password and token secret for the operator to retain, print the values only to standard output, and never create or modify .env."
    - "Wrong passwords, malformed or tampered tokens, and anonymous protected requests are rejected; health, version, and login routes stay public."
    - "Production probes pass only when /healthz returns 200 and anonymous /api/channels returns 401."
  artifacts:
    - "backend/config/config.go enforces environment-aware loads and restrictive file persistence."
    - "backend/api/settings.go exposes redacted views and write-only replacement inputs."
    - "frontend/app/settings-page.tsx never hydrates secret inputs from API responses."
    - "backend/auth/auth_test.go locks the single-admin authentication boundary."
    - "scripts/print-auth-env.sh and scripts/print-auth-env.ps1 emit operator-held credentials without editing deployment files."
    - "scripts/check-production.sh and scripts/check-production.ps1 enforce the production health/auth contract."
  key_links:
    - "cmd/server/main.go creates a file-only baseline config while continuing to run with the environment-effective config."
    - "backend/runtimeconfig/runtime.go reloads through the environment-aware config path before replacing auth, proxy, upstream, and scheduler collaborators."
    - "backend/api/settings.go merges non-secret values and explicit secret replacements into file-backed state, then backend/runtimeconfig/runtime.go reapplies environment precedence."
    - "frontend/lib/api-types.ts matches the redacted Settings response and write-only replacement request contract."
    - "Production check scripts exercise the same public /healthz and protected /api/channels boundary registered by backend/api/api.go."
---

<objective>
Harden the existing brownfield production configuration and single-admin authentication path so runtime Settings operations cannot weaken environment-owned deployment controls or disclose effective secrets, and so operators can prove the deployed anonymous boundary before release.

Purpose: A production image is not safe merely because auth code exists; environment precedence, secret persistence, public-route exceptions, and deployed response codes must remain correct through the real Settings and Compose workflows.

Output: Tested config/runtime policies, a redacted write-only Settings contract, operator-controlled credential helpers, authentication boundary tests, and deterministic production probes. Preserve the current dirty implementations as the starting point and do not create a walking skeleton.
</objective>

<execution_context>
@C:/Users/owen/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/owen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-automated-gates-production-safety/01-PLAN-OUTLINE.md
@.planning/codebase/CONCERNS.md
@docs/FORK_NOTES.md

<interfaces>
- `config.Load(path)` and `config.LoadWithPath(path)` apply Viper environment bindings; `config.LoadFile(path)` intentionally reads defaults plus YAML only; `config.Save(path, cfg)` is the single YAML persistence boundary.
- `runtimeconfig.Manager.ApplyFromFile()` currently calls the file-only loader before rebuilding auth and updating dispatcher, channel service, scheduler, proxy, and upstream state.
- `GET /api/settings/config`, `PUT /api/settings/config`, and `POST /api/settings/apply` are registered by `backend/api/settings.go` behind `runtimeconfig.Manager.AuthMiddleware()`.
- `auth.Service.Login`, `auth.Service.Verify`, and `auth.Service.Middleware` own constant-time credential checking, HMAC token verification, public-route exceptions, and protected-route rejection.
- `SystemConfigResponse` in `frontend/lib/api-types.ts` is consumed by `frontend/app/settings-page.tsx`; response types must not imply that plaintext secrets are readable.
- Compose supplies `APP_SECRET`, `AUTH_ENABLED`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`, and `AUTH_TOKEN_SECRET` as environment-owned production values. Authentication remains opt-in for local/internal deployments; this plan must not invent a password or silently enable it.
</interfaces>
</context>

## Artifacts this phase produces

- Environment-safe startup and runtime configuration persistence with restrictive file permissions.
- A Settings API/UI contract that reports secret presence without returning secret values and accepts explicit write-only replacements.
- Cross-platform credential generation and deployed anonymous-boundary probes.
- Automated regression evidence for login, token integrity, route access, Settings redaction, persistence, and environment precedence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve environment authority and secure config persistence</name>
  <files>backend/config/config.go, backend/config/config_test.go, backend/runtimeconfig/runtime.go, backend/runtimeconfig/runtime_test.go, cmd/server/main.go</files>
  <read_first>
    <file>backend/config/config.go</file>
    <file>backend/config/config_test.go</file>
    <file>backend/runtimeconfig/runtime.go</file>
    <file>backend/runtimeconfig/runtime_test.go</file>
    <file>cmd/server/main.go</file>
    <file>docker-compose.yml</file>
    <file>.env.example</file>
  </read_first>
  <behavior>
    - With conflicting YAML and environment values, startup and `Manager.ApplyFromFile` use the environment values for auth and every currently bound deployment key.
    - Creating a missing config file writes defaults/file-owned values, never the effective APP_SECRET, ADMIN_PASSWORD, AUTH_TOKEN_SECRET, database password, or proxy password sourced from the environment.
    - Saving either a new or existing config leaves the file at owner read/write permissions on POSIX systems.
    - Applying invalid effective auth configuration fails before replacing the current scheduler/auth state.
  </behavior>
  <action>Write the failing config and runtime tests first. Keep `LoadFile` as the explicit YAML-only primitive, but make `Manager.ApplyFromFile` use the environment-aware load path so Settings apply cannot replace Compose-owned auth or other bound values with lower-precedence YAML. Change the missing-config branch in `cmd/server/main.go` to create and save a file-only baseline while the process continues using the already-loaded effective config; do not serialize the effective startup object because it contains environment secrets. Harden `config.Save` so new and pre-existing config files are forced to mode `0600` on POSIX systems, with Windows-compatible tests that skip unsupported permission assertions. Preserve the existing public signatures unless a narrowly named persistence helper makes the source distinction explicit. Keep `APP_SECRET` stable and required; never generate a replacement in application startup.</action>
  <verify>
    <automated>go test ./backend/config ./backend/runtimeconfig -count=1</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Runtime apply demonstrably retains environment-owned auth and deployment values over conflicting YAML.</criterion>
    <criterion>Initial config creation cannot copy process secrets into config.yaml.</criterion>
    <criterion>Both newly created and previously permissive config files become `0600` where POSIX mode bits apply.</criterion>
    <criterion>A failed apply leaves the previously running auth and scheduler collaborators intact.</criterion>
  </acceptance_criteria>
  <done>Environment precedence survives runtime apply, startup never persists effective environment secrets, config files are restrictive on POSIX, and focused config/runtime tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace readable Settings secrets with redacted write-only fields</name>
  <files>backend/api/settings.go, backend/api/settings_test.go, frontend/lib/api-types.ts, frontend/app/settings-page.tsx</files>
  <read_first>
    <file>backend/api/settings.go</file>
    <file>backend/api/settings_test.go</file>
    <file>backend/config/config.go</file>
    <file>backend/runtimeconfig/runtime.go</file>
    <file>frontend/lib/api-types.ts</file>
    <file>frontend/app/settings-page.tsx</file>
  </read_first>
  <behavior>
    - Settings GET reflects effective non-secret auth/proxy state but returns only configured-state booleans for passwords and token secrets; secret value keys are absent from the response.
    - Settings PUT with no replacement field preserves existing file-backed secret values and never copies environment-derived values into YAML.
    - A non-empty explicit password or token-secret replacement is persisted as an operator-requested change; an empty UI field means unchanged, not clear or replace with an unknown value.
    - The UI never hydrates password or token-secret controls from a GET response, clearly distinguishes configured from replacement state, and keeps the existing save/apply workflow.
  </behavior>
  <action>Write API tests for response redaction, file-secret preservation, environment-secret non-persistence, and explicit replacements before changing handlers. Replace direct `config.AuthConfig` and `config.ProxyConfig` exposure with dedicated Settings view/input DTOs: views expose non-secret values plus `passwordConfigured`/`tokenSecretConfigured` indicators, while inputs use optional write-only replacement fields. Load effective config only for the redacted GET view; for PUT, start from `LoadFile` state and merge non-secret fields plus only non-empty explicit replacements so environment values are neither returned nor copied to disk. Update `SystemAuthConfig`, `SystemProxyConfig`, request types, and the settings form so transient replacement controls are separate from configured indicators and the exported JSON remains secret-free by construction. Do not expose database or application secrets and do not add a second privileged endpoint.</action>
  <verify>
    <automated>go test ./backend/api -run TestSettings -count=1 &amp;&amp; pnpm --dir frontend exec tsc --noEmit --incremental false</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Serialized Settings responses contain no plaintext admin, token-signing, proxy, database, or application secret.</criterion>
    <criterion>Saving an unrelated setting with environment-managed credentials leaves those credentials absent from YAML and effective after apply.</criterion>
    <criterion>Existing file-managed secrets survive a blank replacement, while a deliberate non-empty replacement is accepted and protected by the config file permission policy.</criterion>
    <criterion>The frontend compiles against the redacted response/write-only request contract and does not cache returned secret values because none are returned.</criterion>
  </acceptance_criteria>
  <done>Settings GET is secret-free, PUT preserves or explicitly replaces file-backed secrets without copying environment values, the UI uses transient replacement fields, and API/type checks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock authentication boundaries and operator-controlled production probes</name>
  <files>backend/auth/auth_test.go, scripts/print-auth-env.sh, scripts/print-auth-env.ps1, scripts/check-production.sh, scripts/check-production.ps1</files>
  <read_first>
    <file>backend/auth/auth.go</file>
    <file>backend/auth/auth_test.go</file>
    <file>backend/api/api.go</file>
    <file>backend/api/auth.go</file>
    <file>backend/runtimeconfig/runtime.go</file>
    <file>scripts/print-auth-env.sh</file>
    <file>scripts/print-auth-env.ps1</file>
    <file>scripts/check-production.sh</file>
    <file>scripts/check-production.ps1</file>
    <file>docs/FORK_NOTES.md</file>
  </read_first>
  <behavior>
    - Wrong username/password pairs and malformed, expired, wrong-subject, or signature-tampered tokens are rejected without accepting partial or alternate credentials.
    - Anonymous health, version, and login requests remain public; anonymous protected API requests return 401; valid bearer credentials reach protected routes.
    - Both credential helpers use a cryptographic random source for a non-empty strong password and token secret, emit a pasteable block only to standard output, and leave an existing sentinel `.env` byte-for-byte unchanged. If a caller supplies a password, that exact operator-retained password is used.
    - Both production probes use bounded requests without credentials and exit successfully only for health 200 plus anonymous channels 401; connection failures, redirects, 200/403/5xx anonymous channel responses, or unhealthy status fail without printing secrets.
  </behavior>
  <action>Extend the existing dirty `backend/auth/auth_test.go` rather than replacing it, covering expiration, wrong subject, malformed tokens, exact public routes, and anonymous/authenticated protected access through the established middleware contract. Retain the dirty cross-platform helpers as the baseline, but remove the shell helper's predictable `ChangeMe` fallback and fail clearly when a cryptographic random source cannot produce both values. The helpers must never write `.env`, silently enable auth, invent and discard a password, or print secrets anywhere except their explicit operator-facing stdout. Harden the Bash and PowerShell production probes to reject redirects and every status pair except `/healthz=200` and unauthenticated `/api/channels=401`, with connection/overall timeouts and no Authorization header. Preserve local/internal auth-disabled behavior; production safety is enforced by the explicit operator credential workflow and the blocking probe, not by silently changing defaults.</action>
  <verify>
    <automated>go test ./backend/auth -count=1 &amp;&amp; bash -n scripts/print-auth-env.sh scripts/check-production.sh &amp;&amp; powershell -NoProfile -ExecutionPolicy Bypass -Command "$null = [scriptblock]::Create((Get-Content -Raw './scripts/print-auth-env.ps1')); $null = [scriptblock]::Create((Get-Content -Raw './scripts/check-production.ps1'))"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Authentication tests reject wrong credentials and every tested invalid-token class while preserving only the intended public routes.</criterion>
    <criterion>Credential helper output always contains an operator-known password and non-empty token secret, and neither helper modifies `.env`.</criterion>
    <criterion>Production checks fail closed for every response combination except health 200 plus anonymous channels 401.</criterion>
    <criterion>No helper or test output contains a real deployment secret; fixtures and placeholders are clearly non-production.</criterion>
  </acceptance_criteria>
  <done>Authentication boundary tests pass, credential helpers are cryptographically random and stdout-only, and both production probes fail closed outside the exact safe status pair.</done>
</task>

</tasks>

<threat_model>
## Security Level

OWASP ASVS L1. All High-severity threats are blocking and must be mitigated before this plan completes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process environment/YAML to config/runtime | Compose-owned values and file-owned Settings values are combined before constructing live services. |
| Authenticated browser to Settings API | An authenticated operator submits untrusted configuration input and receives a response that must not contain durable secrets. |
| Anonymous network client to Gin API | Public health/version/login traffic and protected management traffic share one HTTP router and dynamic auth middleware. |
| Application to mounted data filesystem | Configuration is persisted beneath the Compose data mount and must not become world-readable. |
| Operator shell to credential/probe helpers | Helpers intentionally handle newly generated credentials or inspect an unauthenticated deployment boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Elevation of Privilege | `config.Load*`, `Manager.ApplyFromFile` | high | mitigate | Task 1 tests conflicting YAML/environment state and requires environment-aware reload before any live collaborator is replaced. |
| T-01-02 | Information Disclosure | startup config creation, Settings GET/PUT, `config.Save` | high | mitigate | Tasks 1-2 keep environment secrets out of YAML, return redacted DTOs, accept only write-only replacements, and force restrictive file permissions. |
| T-01-03 | Spoofing / Elevation of Privilege | `auth.Service`, dynamic auth middleware | high | mitigate | Task 3 tests wrong credentials, token integrity/expiry/subject, exact public routes, anonymous 401, and authenticated protected access. |
| T-01-04 | Tampering | production check scripts | high | mitigate | Task 3 requires credential-free bounded requests, redirect rejection, exact status matching, and nonzero exit on every unsafe state. |
| T-01-05 | Information Disclosure | credential generation scripts | medium | mitigate | Task 3 uses OS cryptographic randomness, stdout-only delivery, no `.env` mutation, no secret logging, and explicit operator retention before auth is enabled. |
| T-01-06 | Repudiation | stateless token logout/revocation | low | accept | Single-operator v1 keeps stateless tokens for upstream compatibility; rotation of the operator-held signing secret plus container restart invalidates outstanding tokens, and TTL remains configurable. |
| T-01-07 | Information Disclosure | browser token in localStorage | medium | accept | This is outside the primary Settings/config boundary and changing to cookies requires CSRF/browser work not present in this phase; v1 retains header-based API use, avoids returning durable Settings secrets, and requires HTTPS at the deployment boundary. |
| T-01-08 | Denial of Service / Spoofing | login endpoint without lockout | low | accept | The single-operator model uses a cryptographically generated high-entropy password; online guessing is infeasible at that entropy, while lockout could deny the sole operator. Network-layer rate limiting remains an explicit deployment defense. |
| T-01-09 | Information Disclosure | legacy query/cookie token extraction | low | accept | Existing compatibility paths remain unchanged; the maintained browser client uses Authorization headers, production documentation requires HTTPS, and no new query-token workflow is introduced. |

## V1 Residual-Risk Disposition

Server-side token revocation, HttpOnly-cookie/CSRF migration, login lockout, and removal of legacy token transports are not silently bundled into this compatibility-focused plan. The accepted rows above document their bounded single-operator v1 rationale; any future change requires its own compatibility and browser/API verification scope. Authentication remaining optional for explicit local/internal deployments is not an accepted production risk: Task 3's production probe blocks release whenever anonymous channel access is not 401.
</threat_model>

<verification>
1. Run `go test ./backend/config ./backend/runtimeconfig ./backend/api ./backend/auth -count=1`.
2. Run `pnpm --dir frontend exec tsc --noEmit --incremental false` and `pnpm --dir frontend lint`.
3. Run `go test ./... -count=1` to confirm official v0.0.6 backend behavior remains intact.
4. Run `git diff --check`, Bash syntax checks, and PowerShell parser checks for all four security helpers.
5. Against a disposable auth-enabled Compose instance with operator-retained test credentials, run the platform production probe and confirm it reports health 200 plus anonymous API 401; rerun against an auth-disabled disposable instance and confirm nonzero exit. Never point this verification at an instance containing real credentials or production data.
</verification>

<success_criteria>
- Settings read/save/apply cannot lower environment precedence or disclose/copy effective environment secrets.
- Config persistence is owner-readable/writable only on POSIX and remains functional on Windows.
- Operators receive generated credentials without automatic `.env` edits and retain the password before choosing to enable authentication.
- Authentication rejects wrong credentials, invalid tokens, and anonymous protected requests while keeping only health, version, and login public.
- Both supported production probes block any deployment that is unhealthy or anonymously exposes `/api/channels`.
- Every High threat in the register has executable mitigation evidence; accepted residual risks are lower severity, explicitly bounded, and do not permit an auth-disabled production release.
- No Phase 2 OneBot/browser UAT, Phase 3 backup/recovery/browser E2E, or Phase 4 clean-build/release/tag work is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-automated-gates-production-safety/01-01-SUMMARY.md` when done.
</output>
