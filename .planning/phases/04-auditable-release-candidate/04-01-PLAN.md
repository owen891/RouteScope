---
phase: 04-auditable-release-candidate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release-candidate.ps1
  - scripts/release-candidate.sh
  - scripts/release_candidate_test.go
requirements: [RELS-01]
autonomous: true
---

<objective>
Make clean-checkout Docker build and runtime health verification reproducible.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add candidate build helper</name>
  <files>scripts/release-candidate.ps1, scripts/release-candidate.sh, scripts/release_candidate_test.go</files>
  <action>Build the pinned Dockerfile with a deterministic candidate tag, validate Compose using a non-production APP_SECRET, start an isolated Compose project when Docker is available, and require /healthz=200. Keep data isolated and never print secrets.</action>
  <verify>go test ./scripts -run 'Release|Candidate' -count=1</verify>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Contract-test release helper</name>
  <files>scripts/release_candidate_test.go</files>
  <action>Verify shell/PowerShell command contracts, clean-build context, image tag validation, and secret-redaction boundaries without requiring a registry.</action>
  <verify>go test ./scripts -run 'Release|Candidate' -count=1</verify>
</task>
</tasks>

<acceptance_criteria>
  - A clean checkout can build the Dockerfile with the frozen pnpm lockfile.
  - Candidate verification uses an isolated data directory and requires health 200.
  - Missing Docker/daemon produces an explicit blocked result, never a false pass.
</acceptance_criteria>
