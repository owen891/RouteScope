---
phase: 03-recoverable-deployment-browser-verification
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/e2e/*
  - frontend/playwright.config.ts
  - package.json
  - scripts/verify.ps1
  - scripts/verify.sh
  - docs/FORK_NOTES.md
requirements: [QUAL-04]
autonomous: true
---

<objective>
Add browser-level regression coverage for auth protection, import preview, failure recovery, notification form, and production probe status.
</objective>

<must_haves>
  - Tests run against a deterministic local backend fixture without real credentials or external services.
  - Auth gate verifies public health/version/login and protected API behavior.
  - Import preview verifies conflict policy and row-level error rendering without uploading the raw backup.
  - Failure recovery verifies filtered targets and per-item batch results.
  - QQ form verifies group/private and Bearer/query controls; production probe UI verifies 200/401/open/error states.
</must_haves>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add deterministic Playwright harness</name>
  <files>frontend/e2e/*, frontend/playwright.config.ts, package.json</files>
  <action>Use a local Vite/backend fixture with intercepted API responses. Keep tests independent, avoid real secrets, and capture screenshots/traces only on failure.</action>
  <verify>pnpm.cmd --dir frontend test:e2e</verify>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire browser checks into quality entrypoints</name>
  <files>scripts/verify.ps1, scripts/verify.sh, docs/FORK_NOTES.md</files>
  <action>Add the browser gate after unit/build checks with an explicit opt-out only for environments lacking a browser binary; CI must install/use the pinned browser and remain blocking.</action>
  <verify>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1 && SKIP_INSTALL=true ./scripts/verify.sh</verify>
</task>
</tasks>

<acceptance_criteria>
  - Browser tests fail on auth bypass, import preview regression, broken recovery feedback, QQ form regression, or production probe state regression.
  - The same browser command runs locally and in CI with deterministic fixtures.
</acceptance_criteria>
